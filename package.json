{
  "name": "ITP.js",
  "version": "1.0.0",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-stage-2": "^6.24.1",
    "fibers": "^2.0.2",
    "flow-bin": "^0.41.0",
    "istanbul": "~0.2.4",
    "coveralls": "~2.7.1"
  },
  "scripts": {
    "flow": "flow",
    "test": "flow && babel src -d lib && mocha --compilers js:babel-core/register",
    "build": "babel src -d lib",
    "buildtest" : "babel src -d lib && mocha --compilers js:babel-core/register",
    "test-travis": "babel src -d lib && ./node_modules/istanbul/lib/cli.js cover ./node_modules/mocha/bin/_mocha  -- -R spec ./test/* --compilers js:babel-core/register"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "jsverify": "^0.8.3",
    "mocha": "^5.2.0",
    "npm": "^6.1.0",
    "parsimmon": "^1.7.3",
    "underscore": "^1.9.1"
  }
}
